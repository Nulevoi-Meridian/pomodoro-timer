export interface ButtonType {
  start: string;
  pause: string;
  continue: string;
  startNew: string;
}

export const START_BUTTON: ButtonType = {
  start: "Start",
  pause: "Pause",
  continue: "Continue",
  startNew: "Start new",
};
export const RESET_BUTTON = "Reset";
export const SAVE_BUTTON = "Save";
